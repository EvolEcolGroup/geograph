% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/closestNodeFnn.R
\name{closestNodeFnn}
\alias{closestNodeFnn}
\title{Sped up version of closestNode using FNN::get.knnx}
\usage{
closestNodeFnn(index, loc)
}
\arguments{
\item{index}{A list containing the nodes and their corresponding Cartesian coordinates,
typically created by \code{buildNnIndex}}

\item{loc}{A matrix or data frame of coordinates (longitude, latitude) to find the closest nodes for}
}
\value{
A named vector of the closest nodes corresponding to the input coordinates
}
\description{
This function finds the closest node in a gGraph to a given set of coordinates
using the FNN package for fast nearest neighbor searches.
It takes an index built by \code{buildNnIndex} and a set of coordinates,
converts them to Cartesian coordinates, and returns the closest nodes.
}
\examples{
idx.fnn <- buildNnIndex(worldgraph.40k)
result.fnn <- closestNodeFnn(idx.fnn, hgdp@coords)
}
