% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assignByPolygon.R
\name{assignByPolygon}
\alias{assignByPolygon}
\alias{assignByPolygon,matrix-method}
\alias{assignByPolygon,data.frame-method}
\alias{assignByPolygon,numeric-method}
\alias{assignByPolygon,list-method}
\alias{assignByPolygon,gGraph-method}
\alias{assignByPolygon,gData-method}
\title{Assign node attributes from a polygon layer}
\usage{
assignByPolygon(x, ...)

\S4method{assignByPolygon}{matrix}(x, layer = "world", attr = "all", ...)

\S4method{assignByPolygon}{data.frame}(x, layer = "world", attr = "all", ...)

\S4method{assignByPolygon}{numeric}(x, layer = "world", attr = "all", ...)

\S4method{assignByPolygon}{list}(x, layer = "world", attr = "all", ...)

\S4method{assignByPolygon}{gGraph}(x, layer = "world", attr = "all", ...)

\S4method{assignByPolygon}{gData}(x, layer = "world", attr = "all", ...)
}
\arguments{
\item{x}{a matrix, \code{data.frame}, list, valid \code{\link{gGraph}}, or valid \code{\link{gData}}
object. For matrix and data.frame, input must have two columns giving
longitudes and latitudes. For list, input must have two components being
vectors of longitudes and latitudes.}

\item{...}{further arguments passed to other methods (currently unused).}

\item{layer}{a shapefile of class \code{sf} (see \code{\link[sf:st_read]{sf::st_read}} to import a
GIS shapefile). Alternatively, \code{"world"} to use the built-in world
shapefile.}

\item{attr}{a character vector of variable names to extract from the layer.
Use \code{"all"} to extract all available variables.}
}
\value{
For matrix, data.frame, or list input: a \code{data.frame} with one row
per location and one column per requested variable. For \code{\link{gGraph}} input:
a \code{\link{gGraph}} object with new node attributes added to \verb{@nodes.attr}. For
\code{\link{gData}} input: a \code{\link{gData}} object with new data added to \verb{@data}.
}
\description{
The function \code{assignByPolygon} uses information from a GIS polygon
shapefile to define node attributes. For each node, information is
retrieved from the layer and assigned to that node.
}
\details{
Nodes can be specified as a matrix, \code{data.frame}, list, \code{\link{gGraph}}, or
\code{\link{gData}} object. Outputs match the input format.

The \code{\link{gGraph}} method can be memory-intensive for large graphs
since it assigns attributes to all nodes.
}
\section{Functions}{
\itemize{
\item \code{assignByPolygon(matrix)}: Method for matrix input

\item \code{assignByPolygon(data.frame)}: Method for data.frames input

\item \code{assignByPolygon(numeric)}: Method for numeric vector input

\item \code{assignByPolygon(list)}: Method for numeric list input

\item \code{assignByPolygon(gGraph)}: Method for gGraph objects

\item \code{assignByPolygon(gData)}: Method for gData objects

}}
\note{
The gGraph method should be carefully used, output is going to be heavy.
}
\examples{

plot(worldgraph.10k, reset = TRUE)

## retrieve continent info for all nodes
## (might take a few seconds)
x <- assignByPolygon(worldgraph.10k, layer = "world", attr = "continent")
x
table(getNodesAttr(x, attr.name = "continent"))


## subset Africa
temp <- getNodesAttr(x, attr.name = "continent") == "Africa"
temp[is.na(temp)] <- FALSE
x <- x[temp]
plot(x, reset = TRUE)

}
\seealso{
\code{\link{findLand}} to find which locations are on land.
\code{\link{assignByRaster}} to assign attributes from raster data.
}
