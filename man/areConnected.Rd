% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/areConnected.R
\name{areConnected}
\alias{areConnected}
\title{Test if a set of nodes form a connected set}
\usage{
areConnected(x, nodes)
}
\arguments{
\item{x}{a \link{gGraph} object}

\item{nodes}{a vector of node names}
}
\value{
a single logical value, being TRUE if nodes form a connected set.
}
\description{
This function tests if a set of nodes form a connected set on a
\code{\link{gGraph}} object.
}
\details{
@details
This function is very similar to \code{\link[=isConnected]{isConnected()}}, but it allows
the user to specify a subset of nodes to test for connectivity,
whereas \code{\link[=isConnected]{isConnected()}} tests if all nodes in the object form a
connected set. Note that \code{\link[=isConnected]{isConnected()}} is a method for both
\link{gGraph} and \link{gData} objects, whereas \code{\link[=areConnected]{areConnected()}} is
only a method for \link{gGraph} objects.

This is an implementation of the \code{\link[graph:graph-class]{graph::isConnected()}} function for
data classes in \code{geoGraph}.
}
\examples{
# create a small square graph
test_graph <- makeGrid(25, lon.range = c(1, 5), lat.range = c(1, 5))
# test that the function correctly identifies connected sets
# 1, 9, and 10 are connected
areConnected(test_graph, nodes = c("1", "9", "10"))
# even though they are not neighbours
areNeighbours(V1 = "1", V2 = "9", graph = test_graph)
areNeighbours(V1 = "1", V2 = "10", graph = test_graph)
}
\seealso{
Other connectivity_functions: 
\code{\link{areNeighbours}()},
\code{\link{isConnected,gData-method}},
\code{\link{isReachable}()}
}
\concept{connectivity_functions}
