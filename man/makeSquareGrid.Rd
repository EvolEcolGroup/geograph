% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeSquareGrid.R
\name{makeSquareGrid}
\alias{makeSquareGrid}
\title{Make a new gGraph object from a custom square grid}
\usage{
makeSquareGrid(
  size = NULL,
  n.lon = NULL,
  n.lat = NULL,
  lon.range = NULL,
  lat.range = NULL
)
}
\arguments{
\item{size}{an integer giving the approximate number of nodes of the grid.
The function will attempt to make a square grid of (approximately) this
size.}

\item{n.lon}{the number of longitude coordinates of the grid (i.e., width of
the grid, in number of cells)}

\item{n.lat}{the number of latitude coordinates of the grid (i.e., height of
the grid, in number of cells)}

\item{lon.range, lat.range}{vectors of length two giving the range covered by
the grid, in longitude and latitude, respectively.}
}
\value{
A \linkS4class{gGraph} object.
}
\description{
The function \code{\link{makeSquareGrid}} builds a \code{\link{gGraph}} using a regular square grid for a given area.
If no area is specified, currently plotted area is used. Note that such grid is only valid for
small scales, for cases in which curvature of the surface of the earth can be neglected.
}
\examples{
# by defining the range covered by the grid
squareGraph <- makeSquareGrid(
  size      = 10000,
  lon.range = c(-12, 2),
  lat.range = c(49, 61)
)
squareGraph <- findLand(squareGraph)
squareGraph <- setColors(
  squareGraph,
  col.rules = data.frame(
    habitat = c("sea", "land"),
    color = c("blue", "green")
  )
)
plot(squareGraph, reset = TRUE)

# If no area is specified, currently plotted area is used
geo.zoomin(c(8, 13, 54, 58))
newGraph <- makeSquareGrid(1e3)
newGraph <- findLand(newGraph)
newGraph <- setColors(
  newGraph,
  col.rules = data.frame(
    habitat = c("sea", "land"),
    color = c("blue", "green")
  )
)

## plot the new gGraph
plot(newGraph, reset = TRUE, edge = TRUE)

}
\seealso{
\code{\link{makeHexGrid}} for a hexagonal grid that avoids projection
distortions over large areas. \code{\link{findLand}} to classify nodes as land
or sea after creating the grid.
}
\keyword{methods}
\keyword{utilities}
