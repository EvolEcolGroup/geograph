% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dijkstraBuffer.R
\name{dijkstraBuffer}
\alias{dijkstraBuffer}
\title{Find nodes reachable within a cost threshold}
\usage{
dijkstraBuffer(x, origin, d, res.type = c("nodes", "gGraph"), ...)
}
\arguments{
\item{x}{a valid \code{\link{gGraph}} object with edge costs defined (see
\code{\link{setCosts}}).}

\item{origin}{either a character string naming a node in \code{x}, or a
\code{data.frame}, list, or numeric vector of length 2 giving longitude and
latitude of the origin location.}

\item{d}{numeric. Maximum cumulative cost defining the reachable area.}

\item{res.type}{a character string indicating the output format:
\itemize{
\item \code{"nodes"}: a character vector of reachable node names (default).
\item \code{"gGraph"}: the input \code{\link{gGraph}} with a new logical node attribute
\code{reachable} indicating which nodes fall within the cost threshold.
}}

\item{...}{further arguments passed to other methods (currently unused).}
}
\value{
A character vector of reachable node names when
\code{res.type = "nodes"}, or a \code{\link{gGraph}} object with a new logical node
attribute \code{reachable} when \code{res.type = "gGraph"}.
}
\description{
The function \code{dijkstraBuffer} identifies all nodes reachable from a given
origin within a specified maximum cumulative cost, where the distance is
defined as the cumulative least-cost path over weighted edges.
}
\details{
Internally, the function computes single-source shortest paths using
Dijkstra's algorithm from the origin node and returns all nodes whose
minimum path cost does not exceed the specified threshold.
}
\examples{
## zoom in to an area
plot(rawgraph.10k, reset = TRUE)
geo.zoomin(list(x = c(-6, 38), y = c(35, 73)))
x <- rawgraph.10k[isInArea(rawgraph.10k)]
## get all nodes reachable within cost 10 from node the origin (here Zurich)
zurich <- data.frame(lon = 8.55, lat = 47.37)
## set costs for the graph and calculate the buffer
graph <- setCosts(x, attr.name = "habitat", method = "mean")
x2 <- dijkstraBuffer(graph, origin = zurich, d = 10, res.type = "gGraph")

## plot reachable nodes in dark blue, all others transparent
col.rules <- data.frame(
  reachable = c(TRUE, FALSE),
  color     = c("darkblue", "transparent")
)
plot(x2, col.rules = col.rules, reset = TRUE)

}
\seealso{
\code{\link{dijkstraFrom}} and \code{\link{gPath2dist}} for the underlying path
computations. \code{\link{buffer}} for geographic distance-based buffers.
\code{\link{setCosts}} to define edge costs before running \code{dijkstraBuffer}.

Other dijkstra_methods:
\code{\link[=dijkstraBetween]{dijkstraBetween()}},
\code{\link[=dijkstraFrom]{dijkstraFrom()}},
\code{\link[=gPath2dist]{gPath2dist()}}
}
\concept{dijkstra_methods}
