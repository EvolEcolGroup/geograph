% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{gPath-class}
\alias{gPath-class}
\alias{gPath}
\title{Formal class "gPath"}
\description{
The class \code{gPath} is an S3 class storing the results of shortest path
computations between nodes in a \code{\link{gGraph}} object. Paths are computed
using Dijkstra's algorithm via the RBGL package and represent the
minimum-cost routes connecting pairs of nodes in the graph.
}
\details{
\code{gPath} objects are primarily created as outputs from \code{\link{dijkstraFrom}},
\code{\link{dijkstraBetween}}, and \code{\link{polygonBetween}} applied to \code{\link{gGraph}} or \code{\link{gData}} objects.
The structure is based on the output from RBGL's \code{sp.between} function,
enhanced with geographic coordinate information.
}
\section{Creating gPath objects}{

\code{gPath} objects are created by dijkstra methods. Direct construction is not recommended.
}

\section{Structure}{

A named list where each element represents a path between two nodes,
containing:
\itemize{
\item \code{path_detail}: a character vector of node names from source to
destination.
\item \code{length}: a numeric value giving the total cost of the path.
}

An \code{xy} attribute stores a matrix of spatial coordinates (longitude and
latitude) for all nodes referenced in the paths, with node identifiers
as row names.
}

\examples{
ori <- closestNode(worldgraph.40k, cbind(33, 10))
myPath <- dijkstraFrom(hgdp, ori)

## examine the structure
length(myPath)            # number of paths
myPath[[1]]$path_detail   # nodes in first path
myPath[[1]]$length        # cost of first path
myPath[[1]]$length_detail # details for each step

## get coordinates of nodes in paths
head(attr(myPath, "xy"))

## plot the paths
plot(worldgraph.40k, col = NA, reset = TRUE)
plot(myPath)
}
\seealso{
\code{\link{dijkstraFrom}}, \code{\link{dijkstraBetween}}  and \code{\link{polygonBetween}} to create \code{gPath}
objects. \code{\link{plot.gPath}} to visualize paths. \code{\link{gPath2dist}} to extract
distances. \code{\link[=gGraph-class]{gGraph}} and \code{\link[=gData-class]{gData}} for related classes.
}
