% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buffer.R
\name{buffer}
\alias{buffer}
\alias{buffer-methods}
\alias{buffer,gGraph-method}
\alias{buffer,gData-method}
\title{Compute buffers around locations for gGraph and gData objects}
\usage{
\S4method{buffer}{gGraph}(x, nodes, d, res.type = c("nodes", "gGraph"), ...)

\S4method{buffer}{gData}(x, d, res.type = c("nodes", "gData", "gGraph"), ...)
}
\arguments{
\item{x}{a valid \linkS4class{gGraph} or \linkS4class{gData} object.}

\item{nodes}{a character vector identifying the nodes around which buffers
should be computed.}

\item{d}{the radius of the buffer, in km.}

\item{res.type}{the type of result that should be returned (see section
\code{value}.}

\item{\dots}{further arguments passed to specific methods.}
}
\value{
The output depends on the value of the argument \code{res.type}:\cr
\itemize{
\item \code{nodes}: a vector of characters identifying the nodes of the
buffers.\cr
\item \code{gGraph}: a \linkS4class{gGraph} object with a new attribute "buffer"
(TRUE: within buffers; FALSE: outside buffers), and new color rules for this
attribute in \code{@meta$buf.colors}.\cr
\item \code{gData}: a \linkS4class{gData} object including all the nodes of the
buffers.\cr
}
}
\description{
The generic function \code{buffer} finds buffers around specified locations
of a \linkS4class{gGraph} or a \linkS4class{gData} object. Different format
for the output are available.
}
\details{
The computed buffers are sets of nodes lying within a given distance of
specified locations. All nodes of a buffer need to be connected to the
location they surround.
}
\examples{
#### gGraph example ####
## Subset gGraph to Europe
x <- rawgraph.10k[isInArea(worldgraph.10k, reg = list(x = c(-10, 50), y = c(35, 70)), quiet = TRUE)]

## identify one node
node <- closestNode(x, data.frame(lon = 12, lat = 50))

## find a buffer
buffer(x, node, 1000)
buf500km <- buffer(x, node, 1000, res.type = "gGraph")
plot(buf500km, col.rules = buf500km@meta$buf.colors, reset = TRUE)

#### gData example ####

## retain a subset of hgdp
x <- hgdp[27:30]
plot(x, reset = TRUE, col.g = "lightgrey", pch.node = 20)
buf.400 <- buffer(x, 400, res.type = "gData")
points(buf.400, col.node = "gold")

}
\keyword{methods}
\keyword{utilities}
