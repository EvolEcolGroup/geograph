% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assignByRaster.R
\name{assignByRaster}
\alias{assignByRaster}
\title{Assign raster values to graph nodes}
\usage{
assignByRaster(
  graph,
  raster,
  layer.name = "raster_points",
  fun = "mean",
  na.rm = TRUE,
  ...
)
}
\arguments{
\item{graph}{a \code{\link{gGraph}} object.}

\item{raster}{a \code{SpatRaster} object (from the \code{terra} package).}

\item{layer.name}{a character string giving the name of the new node
attribute. Defaults to \code{"raster_points"}.}

\item{fun}{a function or character string specifying how to summarize
raster values within each node. Built-in options are \code{"mean"}, \code{"max"},
\code{"min"}, \code{"median"}, \code{"sd"}, \code{"any"}, and \code{"all"}. Alternatively, pass
any function that takes a numeric vector and returns a scalar. Defaults
to \code{"mean"}.}

\item{na.rm}{logical. Whether to remove \code{NA} values before summarizing.
Defaults to \code{TRUE}.}

\item{...}{additional arguments passed to \code{fun}.}
}
\value{
A \code{\link{gGraph}} object with a new scalar node attribute named
\code{layer.name}.
}
\description{
The function \code{assignByRaster} takes a \code{\link{gGraph}} object and a
\code{SpatRaster}, assigns each raster cell to the nearest graph node, and
collapses the values to a single scalar per node using a summary function.
}
\details{
This function is memory-intensive for high-resolution rasters or
large graphs. Once values are assigned, further node attributes can be
derived using \code{\link{setNodesAttr}}.
}
\examples{

## Make a new gGraph without any nodes attribute
geo.box <- c(xmin = -10, xmax = 30, ymin = 35, ymax = 60)
ggraph <- makeHexGrid(geo.box, spacing = 1000) 

## Create a synthetic raster of random elevation values over the same region.
set.seed(42)
r <- terra::rast(
  xmin = geo.box["xmin"], xmax = geo.box["xmax"],
  ymin = geo.box["ymin"], ymax = geo.box["ymax"],
  resolution = 5, crs = "EPSG:4326"
)
terra::values(r) <- runif(terra::ncell(r)) 

## assign mean raster value per node
ggraph <- assignByRaster(ggraph, r, layer.name = "elevation", fun = "mean") 

## assign standard deviation per node (useful for ruggedness)
ggraph <- assignByRaster(ggraph, r, layer.name = "ruggedness", fun = "sd")

}
\seealso{
\code{\link{setNodesAttr}} to set node attributes manually.
\code{\link{assignByPolygon}} to assign attributes from GIS shapefiles.
}
