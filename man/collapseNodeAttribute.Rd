% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapseNodeAttribute.R
\name{collapseNodeAttribute}
\alias{collapseNodeAttribute}
\title{Collapse a list-based node attribute into a scalar node attribute}
\usage{
collapseNodeAttribute(
  graph,
  attribute,
  fun = c("mean", "max", "min", "median", "sd", "any", "all"),
  na.rm = TRUE,
  replace = TRUE,
  ...
)
}
\arguments{
\item{graph}{A \code{\link{gGraph}} object.}

\item{attribute}{Character string giving the name of the node attribute
to collapse.}

\item{fun}{A function or a character string specifying how to collapse values.
Built-in options include  \code{min},  \code{max},  \code{mean},
\code{median},  \code{sd},  \code{any}, and  \code{all}.}

\item{na.rm}{Logical; whether to remove \code{NA} values before collapsing.
Defaults to \code{TRUE}.}

\item{replace}{Logical; if \code{TRUE} (default), replaces the original
list-based node attribute with the collapsed vector. If \code{FALSE},
the collapsed vector is returned but the graph is left unchanged.}

\item{...}{Additional arguments passed to \code{fun}.}
}
\value{
If \code{replace = TRUE}, a modified \code{\link{gGraph}} object with the
collapsed attribute replacing the original one.

If \code{replace = FALSE}, a vector of collapsed values (one per node).
}
\description{
This function collapses a list-based node attribute
(e.g. raster values assigned to each node) into a single scalar value per node.
Optionally replaces the original attribute to reduce memory usage.
}
\examples{
# create a small graph over Europe
geo.box <- c(xmin = -10, xmax = 30, ymin = 35, ymax = 60)
ggraph <- createNewGraph(geo.box, spacing = 1000)
# create a matching synthetic raster and assign it to the graph
r <- terra::rast(
  xmin = geo.box["xmin"], xmax = geo.box["xmax"],
  ymin = geo.box["ymin"], ymax = geo.box["ymax"],
  resolution = 5,
  crs = "EPSG:4326"
)
terra::values(r) <- runif(terra::ncell(r))
ggraph <- assignRasterPoints(ggraph, r, layer.name = "elevation")
# collapse the elevation attribute to the mean value per node (replaces the list)
ggraph <- collapseNodeAttribute(ggraph, attribute = "elevation", fun = "mean")
}
